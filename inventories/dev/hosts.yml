---
all:
  children:
    servers:
      hosts:
        dev-server-01:
          ansible_host: 54.xx.xx.xx
          ansible_user: ubuntu
          ansible_port: 2222
          ansible_ssh_private_key_file: "~/.ssh/key.pub"  # adapte si besoin
# ansible-playbook -i inventories/dev playbooks/ssh_hardening.yml --check --ask-pass --ask-become-pass